name: TestingConversion

on: [pull_request]

jobs:

  TestingMaster:
    name: Checking Conversion of Cloud-Hosted-
    runs-on: ubuntu-latest
  
    steps:

      - name: Testing with Maven
        run: echo "heloo"